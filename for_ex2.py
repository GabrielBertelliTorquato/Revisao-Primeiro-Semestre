# Calcular o imposto sobre vários valores

lista_precos = [10, 10, 200, 50, 300]
for preco in lista_precos: # Para cada preço dentro da minha lista de preços
    imposto = preco * 0.1 # 0.1 é a mesma coisa que 10%
    print(imposto)